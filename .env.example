# Copy this file to `.env` and fill in the values.
# `.env` is gitignored and holds your local secrets.
#
# When a variable is empty or missing, the app falls back to sensible defaults:
#   - API_URL  → https://api-priora.quipu.club
API_URL=
